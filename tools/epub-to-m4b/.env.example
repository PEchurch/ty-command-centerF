# Copy to .env and fill in the key(s) for whichever --backend you use,
# then `export $(grep -v '^#' .env | xargs)` before running the tool
# (or load it however your shell/tooling prefers).

OPENAI_API_KEY=
ELEVENLABS_API_KEY=
INWORLD_API_KEY=
